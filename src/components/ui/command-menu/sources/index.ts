// Barrel for the four built-in command-menu sources. Add new sources here
// and register them in <CommandMenuProvider/>.

export { useAppActionsSource } from "./app-actions";
export { useAppNavigationSource } from "./app-navigation";
export { useAppFilesSource } from "./app-files";
export { useAppPluginsSource } from "./app-plugins";
